import React from "react";

const Header = () => {
    return (
        <header>
            <h1>Voting App</h1>
            <button>Sign in</button>
            <button>Sign up</button>
        </header>
    )
}

export default Header;